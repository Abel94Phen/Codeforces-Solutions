def merge_sort(nums, left, right):
    # the sorting is basically swapping left and right pairs
    # need a good way to do it.abs
    return
    
    
t = int(input())
for _ in range(t):
    length = int(input())
    array = list(map(int, input().split()))
    
